memory_sources = []

memory_sources += files(
  'internal/helpers.c',
  'linear_allocator.c',
  'pool_allocator.c',
)

memory_internal_includes = include_dirs('internal')

sources += memory_sources
internal_includes += memory_internal_includes
